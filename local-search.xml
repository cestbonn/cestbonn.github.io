<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Machine Learning（二）</title>
    <link href="/2022/02/05/MachineLearning2/"/>
    <url>/2022/02/05/MachineLearning2/</url>
    
    <content type="html"><![CDATA[<h1 id="linear-regression">Linear regression</h1><p>对不同的<span class="math inline">\(x\)</span>，有：单变量，多元。<br> 对不用的<span class="math inline">\(y\)</span>，有：线性回归，广义线性回归。<br> 对任务而言，有：回归任务，分类任务。是通过不同的<span class="math inline">\(g(y)\)</span>实现的。即我们认为<span class="math inline">\(y\)</span>到<span class="math inline">\(x\)</span>是非线性的，同时坐标轴<span class="math inline">\(x\)</span>是线性的，所以, <span class="math display">\[y&lt;-(非线性g^{-1}(\cdot))-z&lt;-(线性回归)-x\]</span> <span class="math display">\[g(y)=f(x)\]</span> 在正常逻辑中，我们习惯<span class="math inline">\(y=g^{-1}(f(x))\)</span>。但是为了莫名其妙地强调“联系”，即<span class="math inline">\(g(y)=f(x)\)</span>。我们在逻辑回归中，<span class="math inline">\(g(y)\)</span>就为对数几率函数。</p><h3 id="单变量线性回归">1. 单变量线性回归</h3><p><span class="math display">\[y_i=wx_i+b\]</span></p><h3 id="多元线性回归">2. 多元线性回归</h3><p><span class="math inline">\(x\)</span>不是标量，是多维的 <span class="math display">\[y_i=\textbf{w}^T\textbf{x}_i+b=\hat{\textbf{w}}^T\hat{\textbf{x}}_i\]</span> 有最优解， <span class="math display">\[\hat{\textbf{w}}^*=(\textbf{X}^T\textbf{X})^{-1}\textbf{X}^T\textbf{y}\]</span></p><ul><li><p><span class="math inline">\(\textbf{X}^T\textbf{X}\)</span>满秩<br> <span class="math inline">\(\hat{\textbf{w}}^*\)</span>有唯一解。</p></li><li><p><span class="math inline">\(\textbf{X}^T\textbf{X}\)</span>不满秩<br> <span class="math inline">\(\hat{\textbf{w}}^*\)</span>有很多解。我们可以通过在目标函数中加入正则化项，选择出其中的某个解。</p></li></ul><h3 id="广义线性回归">3. 广义线性回归</h3><p><span class="math inline">\(x\)</span>与<span class="math inline">\(y\)</span>的关系可能是非线性的，那么我们先把<span class="math inline">\(y\)</span>用反非线性函数映射回来，再用<span class="math inline">\(x\)</span>去线性回归，即 <span class="math display">\[y=g^{-1}(\textbf{w}^T\textbf{x}+b)\]</span> <span class="math inline">\(g(\cdot)\)</span>叫link function。</p><h3 id="二分类">4. 二分类</h3><h4 id="a.-对数几率回归逻辑回归logistic-regression">a. 对数几率回归，逻辑回归，logistic regression</h4><p><span class="math display">\[g(y)=f(x)\Rightarrow ln\frac{y}{1-y}=\textbf{w}^T\textbf{x}+b\]</span></p><h4 id="b.-对数几率回归的概率版本">b. 对数几率回归的概率版本</h4><p>在对数几率回归中，<span class="math inline">\(g^{-1}(f(x))\)</span>直接输出为标签<span class="math inline">\(y\)</span>，<span class="math inline">\(g^{-1}(f(x))=y\)</span>。现在，我们可以多一步，先用连接函数+线性回归输出标签的概率<span class="math inline">\(g^{-1}(f(x))=P(y)\)</span>，原因无他，其一是因为logistic函数提供了一个<span class="math inline">\(P\approx1/2\)</span>，其二是因为logistic函数是单调的且只有一个分界点，说明限制了<span class="math inline">\(x\)</span>只有一个分界面。<span class="math inline">\(x\)</span>就是“抛硬币”，<span class="math inline">\(y\)</span>代表正反面，<span class="math inline">\(f(x)\)</span>将“抛硬币”这一行为变成numerical feature，<span class="math inline">\(P(y)\)</span>将“正反面，0或1”用概率模型去诠释，<span class="math inline">\(g^{-1}(\cdot)\)</span>是从<span class="math inline">\(f(x)\)</span>到<span class="math inline">\(P(y)\)</span>的数值mapping函数。</p><p>注意书中式(3.26)具有迷惑性：似然函数=期望。事实上，右边并不是期望，因为<span class="math inline">\(P(x;\beta)\)</span>实为<span class="math inline">\(\beta\)</span>的函数，而不是概率：<span class="math inline">\(P(y=1/0|x)\)</span></p><h4 id="c.-线性判别分析-linear-discriminant-analysis-lda">c. 线性判别分析 Linear Discriminant Analysis (LDA)</h4><p><font color=red>略过，具体看书</font></p><p>将数据投影到线，优化两个目标：1.同类数据尽可能近，2.异类数据中心尽可能远。用中心投影和协方差作为变量，构造<span class="math inline">\(J\)</span>， 再构造拉格朗日乘子法，算出最佳投影线<span class="math inline">\(w\)</span>。</p><p>将LDA用于多分类时，<span class="math inline">\(N\)</span>维数据可以最佳投影到<span class="math inline">\(N-1\)</span>维，所以LDA可以用于降维。</p><h3 id="多分类">5. 多分类</h3><p>多分类有三种：OvO， OvR，和MvM(<font color=red>略过，具体看书</font>)。</p><table><thead><tr class="header"><th>method</th><th>#二分类器</th><th>预测</th><th>缺点</th></tr></thead><tbody><tr class="odd"><td>OvO</td><td><span class="math inline">\(N(N-1)/2\)</span></td><td>丢进所有分类器，再投票</td><td></td></tr><tr class="even"><td>OvR</td><td><span class="math inline">\(N\)</span></td><td>丢进所有分类器，再投票</td><td>类别不平衡！</td></tr></tbody></table><h3 id="类别不平衡问题">6. 类别不平衡问题</h3><ol type="1"><li>对<span class="math inline">\(g(y)\)</span>进行再缩放</li><li>undersampling多的</li><li>oversampling少的（插值）</li><li>阈值移动,threshold-moving(<font color=red>略过，具体google</font>)</li></ol>]]></content>
    
    
    <categories>
      
      <category>Machine Learning</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Machine Learning</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Machine Learning （一）</title>
    <link href="/2022/02/03/MachineLearning/"/>
    <url>/2022/02/03/MachineLearning/</url>
    
    <content type="html"><![CDATA[<h1 id="overfitting">Overfitting</h1><ol type="1"><li>overfitting能完全避免吗？<br> 不能。因为机器学习面对的问题都是<span class="math inline">\(NP\)</span>-hard，如果能避免，那么说明P=NP。因此，只要相信<span class="math inline">\(P\neq NP\)</span>，就无法避免。</li><li>overfitting如何改善？</li></ol><h1 id="split-data">Split Data</h1><ol type="1"><li>hold-out<br> 直接划分数据集为Train和Test</li></ol><ul><li>[stratified sampling] 通过保证各class的比例，保证training和testing的distribution一致</li></ul><ol start="2" type="1"><li>cross validation / k-fold<br> 用于评估某一模型的好坏</li></ol><ul><li>step1. 保持同样分布将数据集分割成k份</li><li>step2. 选取k-1份训练</li><li>step3. 在剩下的一份中测试</li><li>step4. run k times，将在所有测试的结果平均，作为该模型的estimation</li></ul><ol start="3" type="1"><li>bootstrapping<br> 从m个数据的D中采样m个数据作为训练集D‘，取D中不在D’中的元素作为测试集。如此，训练集和测试集都有m个<br> 对小数据集好</li><li>验证集<br> 基于验证集上的性能来进行模型选择和调参</li></ol><h1 id="model-estimation">Model Estimation</h1><ol type="1"><li>error rate&amp;accuracy</li></ol><h4 id="precisionrecall">2. precision&amp;recall</h4><p>How many predictions are correct in all predictions? <span class="math display">\[P_i=\frac{\#类i成功预测}{\#预测为类i}=\frac{TP}{TP+FP}\]</span> How many truths are correctedly predicted in all truths? <span class="math display">\[R_i=\frac{\#类i成功预测}{\#真实为类i}=\frac{TP}{TP+FN}\]</span></p><h4 id="p-r曲线">3. P-R曲线</h4><p>根据softmax值排序，设置阈值k，大于k被记为TP或FP，小于k被记为TN或FN。不同k，从而有不同PR。</p><p>PR曲线是断的，因为PR不可能取到每一个点</p><h4 id="f1">4. F1</h4><p>F1是P和R的调和平均(harmonic mean) <span class="math display">\[\frac{1}{F1}=\frac{1}{2}\cdot (\frac{1}{P}+\frac{1}{R})\Rightarrow F1=\frac{2TP}{N+TP-TN}\]</span></p><h4 id="f_beta">5. <span class="math inline">\(F_\beta\)</span></h4><p>灵活调整对P和R的偏好，是P和R的weighted调和平均。<span class="math inline">\(\beta&gt;1\)</span>时R影响更大；<span class="math inline">\(\beta&lt;1\)</span>时P影响更大；<span class="math inline">\(\beta=1\)</span>时就是F1 <span class="math display">\[\frac{1}{F_\beta}=\frac{1}{1+\beta^2}\cdot (\frac{1}{P}+\frac{\beta^2}{R})\Rightarrow F_\beta = \frac{(1+\beta^2)\times P\times R}{(\beta^2\times P)+R}\]</span></p><h4 id="macro-p-macro-r-macro-f1">6. macro-P， macro-R， macro-F1</h4><p>将多分类变成n个二分类，从而<span class="math inline">\(P_i\)</span>和<span class="math inline">\(R_i\)</span>只从二分类confusion matrix上计算。macro-P，macro-R就是平均。macro-F1由macro-P，macro-R计算</p><h4 id="micro-p-micro-r-micro-f1">7. micro-P， micro-R， micro-F1</h4><p>同样变成n个二分类。再把TP，FP，TN，FN取平均，再计算。</p><h4 id="roc曲线">8. ROC曲线</h4><p>PR曲线是R和R画出的，ROC曲线是TPR和FPR画出的。同样ROC曲线也是断的，要通过<span class="math inline">\(y+\frac{1}{m+}\)</span>和<span class="math inline">\(x+\frac{1}{m-}\)</span>画</p><p>TPR：How many positives are correctedly predicted? 其实就是R。 <span class="math display">\[TPR=\frac{TP}{TP+FN}\]</span> FPR：How many negatives are correctedly predicted? <span class="math display">\[FPR=\frac{FP}{TN+FP}\]</span></p><h4 id="auc">9.AUC</h4><p>AUC就是ROC下的面积</p><h1 id="hypothesis-test">Hypothesis test</h1><p>比较模型A与模型B谁好。详细地，如果在测试集上观察到学习器A比B好，则A的泛化性能是否在统计意义上优于B ，以及这个结论的把握有多大？</p><p>关键就是已知测试误差<span class="math inline">\(\hat{\epsilon}_A, \hat{\epsilon}_B\)</span>, 如何比较<span class="math inline">\(\epsilon_A, \epsilon_B\)</span>？即使不能准确比较，那么我们模糊的比较的结果的把握有多大？</p><h2 id="t-test">1. t-test</h2><p>今天把这个老大难问题解决，还是周志华好，网上从统计角度出发，容易造成误解。</p><p>t-test总体分为三类，one-sample, two-sample, and paired.</p><h3 id="one-sample-t-test">one-sample t-test</h3><p>为了检查样本的均值是否等于特定的值。我们把statement"样本的均值等于特定值" 记作基本假设<span class="math inline">\(H_0\)</span>，t-test的目的就是根据一个t-value，去接受这个假设，或者否定这个假设。</p><p>比如在超市里的薯片标着每袋100g，但你不这么认为，于是买了10袋回来，做one-sample t-test，如果计算得到的t-value大于<span class="math inline">\(t_{critical}\)</span>，那么说明t-test否定了这个假设，你就可以去投诉了.</p><p>在机器学习中，one-sample t-test用来估计一个模型的<strong>性能</strong>。比如我用模型A在test set上跑了100遍， 得到100个误差，记作<span class="math inline">\([\epsilon_A]\)</span>。我们可以直接使用这些误差的平均值，来作为模型A的性能。但是这有个缺点，就是这些误差可能是有先验的，这就导致我们不能完全相信这100个误差。one-sample t-test 就是用来说明，我们有多大的把握，说模型A的误差等于(t-value小于<span class="math inline">\(t_{critical}\)</span>)，或者不等于(t-value大于<span class="math inline">\(t_{critical}\)</span>) 某个值<span class="math inline">\(\epsilon_0\)</span>。有了t-test后，我们就可以说，我们有xx的把握，模型A的误差为<span class="math inline">\(\epsilon_0\)</span>。这就是对模型A的性能评估结果了。</p><p><strong>那么怎样算t-value？ <span class="math inline">\(t_{critical}\)</span> 是什么？为什么大于是否定，小于是接受？你说的这个把握又是什么</strong></p><p>我们把伟大的自然界当作一个上帝模型，它在test set上的设计误差为<span class="math inline">\(\epsilon_0\)</span>， 方差是啥咱不知道。所以，当这个上帝模型在test set上跑100次时，它的100次误差会呈现一个自由度为<span class="math inline">\(len(\text{test})-1\)</span>的t分布。暂时不管什么是自由度和为什么是t分布，如下图a。同时，我们的模型A也会有这样一个关于误差的分布，是正态的。同样，也不要管为什么是正态分布，如下图b。</p><p>one-sample t-test的目的就是我们如何评估这两个分布相似性？如果模型A的分布和上帝模型的很像，那就说明我们的模型很强啦，也就是接受基本假设<span class="math inline">\(H_0\)</span>；反之，则我们的模型很拉跨，也就是拒绝基本假设<span class="math inline">\(H_0\)</span>。接下来就是讲怎样评估相似性了。</p><p>我们取模型A误差分布的均值<span class="math inline">\(\mu\)</span>和方差<span class="math inline">\(\sigma\)</span>，给他做个变换，</p><p><span class="math display">\[t=\frac{\sqrt{n}(\mu-\epsilon_0)}{\sigma}\]</span></p><p>然后我们就可以把这个<span class="math inline">\(t\)</span>给映射到上帝模型误差分布的横轴上了，如图</p><p>接下来我们就可以做比较了。我们先把上帝模型的这个误差分布给它分块：<span class="math inline">\(1-\alpha\)</span> 和 <span class="math inline">\(\alpha\)</span>。这个<span class="math inline">\(\alpha\)</span>就叫做<strong>置信率</strong>，也就是前文说的<strong>把握</strong>。<span class="math inline">\(\alpha\)</span> 还有个名字，叫做p-value。因为他是percentage value...我无语。而<span class="math inline">\(\alpha\)</span>对应的这个t值，就叫做<span class="math inline">\(t_{critical}\)</span>。</p><p>接着，我们就要看映射过来的<span class="math inline">\(t\)</span>。如果<span class="math inline">\(t\)</span>在区间内，那么我们认为模型A所产生的误差的分布，与上帝模型产生的一致，也就是说我们可以认为模型A的statiscal误差就是上帝模型的平均误差，即<span class="math inline">\(\epsilon_0\)</span>。而我们有多大的把握，我们有<span class="math inline">\(1-\alpha\)</span>的把握。</p><p>一般我们给p-value一个定值，常用的是0.05。根据数据的length，我们可以得到自由度，从而得到t分布，从而知道p-value对应的<span class="math inline">\(t_{critical}\)</span>。然后我们计算统计数据的t-value，通过与<span class="math inline">\(t_{critical}\)</span>的对比，就可以判断是否应该接受假设<span class="math inline">\(H_0\)</span>了。</p><p>反过来，有时候我们也会直接计算统计数据的t-value，然后再把它转换为分布图像的百分比，即得到统计数据的p-value。这个p-value越小，说明我们越不能接受。参考值是0.05。</p><h3 id="paired-t-test">paired t-test</h3><p>在机器学习中，paired t-test用来估计两个模型的<strong>性能</strong>是否具有统计学意义上的不同。说人话就是，我用模型A和B在test set上跑了100遍，记录它们的误差: 对于A，有一组<span class="math inline">\([\epsilon_A]\)</span>；对于B，有一组<span class="math inline">\([\epsilon_B]\)</span>。那么我如何通过这两组数据判断到底是模型A好还是模型B好？我们不能直接比较它们的均值，因为它们可能存在先验。</p><p>机智的我们事实上可以转换这个问题。我们把A和B的数据相减，作为新数据。那么现在问题就变成了，评估新数据分布，与均值为0方差未知的t分布，的相似度了吗？这不就是一个one-sample t-test了吗。</p><h3 id="option">option</h3><p>对于所有三种t-test，根据不同的需求，我们可以特殊定制。</p><ul><li>two-tail，one-tail<br> 双尾即两边都有阴影，判断是否相等的假设；单尾即只有一边有阴影，判断有大小的假设</li></ul><h2 id="binomial-test">2. binomial test</h2><p>二项误差就更简单了。现在我们在测试集上只跑了一遍，只有一个<span class="math inline">\(\epsilon\)</span>，那么我们如何评估它和上帝模型的相似度呢？</p><p>我们首先把它们变成“错误prediction的数量”。那么，<span class="math inline">\(\epsilon n\)</span>就是以<span class="math inline">\(\epsilon_0\)</span>为概率在测试集上采样的结果，而这个采样服从二项分布。于是，我们同样可以设置一个置信率，如果<span class="math inline">\(\epsilon n\)</span>小于这个置信率对应的错误prediction数量，说明上帝模型有<span class="math inline">\(1-\alpha\)</span>的概率产生比<span class="math inline">\(1-\alpha\)</span>对应的误差更小的误差。</p><p>但是我们不能卡在那个临界点，所以得往概率高的地方挪一挪。于是我们有，能在<span class="math inline">\(1-\alpha\)</span>的置信率下，相信模型A背后的泛化误差，等于上帝模型的误差<span class="math inline">\(\epsilon_0\)</span>。</p><p>Step</p><ul><li>美好地yy一个<span class="math inline">\(\epsilon_0\)</span>。规定置信度<span class="math inline">\(1-\alpha\)</span>, 并由<span class="math inline">\(\epsilon_0\)</span>和<span class="math inline">\(\alpha\)</span>算出/查表得出<span class="math inline">\(\bar{\epsilon}_0\)</span>。</li><li>如果<span class="math inline">\(\hat{\epsilon}_A\leq \bar{\epsilon}_0\)</span>，那么说明：我们可以以<span class="math inline">\(1-\alpha\)</span>的置信度认为模型A的泛化误差不会大于那个yy的<span class="math inline">\(\epsilon_0\)</span></li></ul><h2 id="bias-variance-noise">Bias, variance, noise</h2><p><span class="math inline">\(y_D\)</span>是训练集真实label，<span class="math inline">\(y\)</span>是测试集真实label，<span class="math inline">\(f(x;D)\)</span>是模型在D上训练后在测试集上的预测label，<span class="math inline">\(\bar{f}(s)=\mathbb{E}_D[f(x;D)]\)</span>是算法的期望预测。</p><ul><li><p>Bias，算法的能力 <span class="math display">\[bias^2(x)=(\bar{f}(x)-y)^2\]</span></p></li><li><p>Variance，数据在不同训练集中的分布平均度，即数据扰动 <span class="math display">\[var(x)=\mathbb{E}_D[(f(x;D)-\bar{f}(x))^2]\]</span></p></li><li><p>Noise，训练的自生难度 <span class="math display">\[\epsilon^2=\mathbb{E}_D[(y_d-y)^2]\]</span></p></li></ul><p>模型的期望泛化误差可以计算为 <span class="math display">\[E(f;D)=\mathbb{E}_D[(f(x;D)-y_D)^2]=bias^2(x)+var(x)+\epsilon^2\]</span></p><p>开始训练时模型拉跨，bias高，var低；过拟合后bias低，var高；泛化误差则是先高后低最后高。</p>]]></content>
    
    
    <categories>
      
      <category>Machine Learning</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Machine Learning</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ReviewList</title>
    <link href="/2022/02/03/ReviewList/"/>
    <url>/2022/02/03/ReviewList/</url>
    
    <content type="html"><![CDATA[<ol type="1"><li>Attention is all you need</li><li>BERT</li><li>GCN</li></ol>]]></content>
    
    
    <categories>
      
      <category>Paper review</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NLP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Attention is all you need</title>
    <link href="/2022/02/01/Attention/"/>
    <url>/2022/02/01/Attention/</url>
    
    <content type="html"><![CDATA[<h1 id="background">Background</h1><p>The fundamental hypothesis behind NLP is the belief in a universal space called "semantic". Any natural language can be transferred into space on a uniform scale. For the task of machine translation, the steps remained are to <strong>encode</strong> language A to the space, then <strong>decode</strong> the semantic representations back to language B.</p><h3 id="rnn">1. RNN</h3><p>However, there is no way for us to encode/decode sentences directedly as a whole. We need an efficient heuristic to achieve it one word by one word, which is the truth that we read and write from left to right. Therefore, RNN came up. It roughly <strong>accumulates</strong> (encode) the <strong>words relationships</strong> into the current <strong>context representation</strong> as current <strong>hidden state</strong>. The final hidden state <span class="math inline">\(h_m\)</span> is expected to be the vector in "semantic space", representing the entire sentence, and thus target sentences can be generated by decoding <span class="math inline">\(h_l\)</span> sequentially. Meanwhile, RNN has the drawback of precluding paralyzed training in batches for its inherently sequential nature.</p><h3 id="lstm">2. LSTM</h3><p>People find that not all words are important to the context representation so we should not simply accumulate hidden states. LSTM introduces cell states (<span class="math inline">\(c_{i-1})\)</span> to control contributions of components of the <strong>context representation</strong> (<span class="math inline">\(h_i\)</span>): current word (<span class="math inline">\(x_i\)</span>) and previous context (<span class="math inline">\(h_{i-1}\)</span>). Although LSTM was originally designed to solve the problem of information loss when decoding long sentences, it indicates that we should calculate the context representation in a selective way.</p><h3 id="attention">3. Attention</h3><p>Attention is proposed to decode the final context representation selectively in terms of words in the translated sentence. To do it, we need weights between the current input of the decoder and all words in the translated sentence. This process is actually a query-key matching. If a query <span class="math inline">\(q_i\)</span> and a key <span class="math inline">\(k_i\)</span> are well matched, we will consider more information of the value of the key word <span class="math inline">\(v_i\)</span> when decoding word <span class="math inline">\(j\)</span>.</p><p>encoder: <span class="math display">\[x_i\rightarrow \{k_i=h_i, v_i=h_i, h_i\}\]</span> decoder: <span class="math display">\[x_j\rightarrow \{q_j=h_j, c_j, h_j\}\]</span></p><p><strong>Weight (<span class="math inline">\(\alpha_{ij}\)</span>)</strong> between <span class="math inline">\(x_i\)</span> and <span class="math inline">\(x_j\)</span> is, <span class="math display">\[\alpha_{ij}=softmax(align(k_i, q_j))\]</span> <strong>Context representation (<span class="math inline">\(c_j\)</span>)</strong> of words <span class="math inline">\(x_j\)</span> is the weighted sum of <span class="math inline">\(v_i\)</span>, <span class="math display">\[c_{j}=\alpha_{:j}V\ , \ V=[v_1, ..., v_m]^T\ ,\ \alpha_{:j}=[\alpha_{1j},...,\alpha_{mj}]\]</span> <strong>Predicted word (<span class="math inline">\(x_{j+1}\)</span>)</strong> is induced from current hidden state or current context representation, <span class="math display">\[x_{j+1}=softmax(FCNet(h_{j}\ or\ c_j))\]</span> <strong>Hidden state (<span class="math inline">\(s_{j+1}\)</span>)</strong> is calculaed from the predicted current input, previous hidden state, and previous context representaion. <span class="math display">\[h_{j+1}=RNNcell(x_{j+1}, h_{j}, c_{j})\]</span> Now we have <span class="math inline">\(x_{j+1}\)</span> and <span class="math inline">\(h_{j+1}\)</span> to inducde <span class="math inline">\(q_{j+1}\)</span> by repeat above steps. sequences of <span class="math inline">\(x_j\)</span> is the translated result. Comparing the encoder and decoder, we treat hidden states as semantically consistent, but the context representation is word-specific. Also, the latter is only calculated in the decoder. It obeys the basic belief while making a trick about weights to help the network. <a href="https://arxiv.org/pdf/1601.06733.pdf?source=post_page---------------------------">paper</a></p><h3 id="self-attention">4. Self-attention</h3><p>Previously, attention is only calculated when decoding, however, we can also generate weighted context representations while encoding, which is so-called self-attention. It works on one RNN instead of encoder-decoder structure, and is more flexible for downstream tasks. <img src="/img/Attention/selfattention0.png" width="30%" height="30%"> Intuitively, final context representation <span class="math inline">\(c_l\)</span> now embeds context spacially, and the final hidden state <span class="math inline">\(h_l\)</span> just simply accumulates context. So <span class="math inline">\(c_l\)</span> is more powerful than <span class="math inline">\(h_l\)</span> in original RNN for downstream tasks. Noticed that this self-attention is not the popular one, it in fact comes from the first self-attention related <a href="https://arxiv.org/pdf/1409.0473.pdf?utm_source=ColumnsChannel">paper</a>. In transformer, self-attention is re-designed systematically and is an independent block to produce <span class="math inline">\(c\)</span>.</p><h1 id="motivation">Motivation</h1><ol type="1"><li>In previous work, vectors of query, key and value are all based on RNN. However, RNN cannot be trained in paralle for its inherently sequential nature. So why not throw it way?</li></ol><h1 id="design">Design</h1><h3 id="attention-1">1. Attention</h3><p>To give up RNN, we generate query, key, and value vectors for all token embeddings by learnale weight matrix.</p><p>encoder: <span class="math display">\[x_i\rightarrow \{k_i=W_kx_i, v_i=W_vx_i\}\]</span> decoder: <span class="math display">\[x_j\rightarrow \{q_j=W_Qx_j, c_j\}\]</span> Instead of align function, we use dot-product of query and key to estimate the relationships between <span class="math inline">\(x_j\)</span> and <span class="math inline">\(x_i\)</span>. To make it consistent with Python, vectors in lower case are row vectors, and the feature dimention is represented on column. <span class="math display">\[c_j=softmax(q_jK^T)\cdot V\Rightarrow Attention(Q,K,V)=softmax(\frac{QK^T}{\sqrt{d_k}}V) \tag {1}\]</span> <span class="math display">\[x_{j+1}=softmax(FCNet(c_j))\]</span> <span class="math inline">\(q_j \in \mathbb{R}^{1\times d_k}\)</span>, <span class="math inline">\(K \in \mathbb{R}^{m\times d_k}\)</span>, <span class="math inline">\(V \in \mathbb{R}^{m\times d_v}\)</span>, <span class="math inline">\(Q \in \mathbb{R}^{m\times d_k}\)</span></p><h3 id="self-attention-1">2. Self-attention</h3>In this part, <span class="math inline">\(j\)</span> is the same as <span class="math inline">\(i\)</span>.<center>encoder: <span class="math inline">\(x_i\rightarrow \{k_i=W_kx_i, v_i=W_vx_i, q_i=W_Qx_i\}\)</span></center><p><span class="math display">\[c_j=softmax(q_jK^T)\cdot V\]</span> For self-attention on the decoder, the author designed a mask before <span class="math inline">\(softmax\)</span> to mask <span class="math inline">\(v_{j+}\)</span> during training, which is so-called masked self-attention.</p><h3 id="multi-head-attentionself-attention">3. Multi-head Attention/Self-attention</h3><p>Just use multiple independent attention/self-atttention to produce <span class="math inline">\(c\)</span>, then concatenate them. It is integerd into the block of "Multi-Head Attention" in the paper, outputing context representation (Attention) <span class="math inline">\(C=[c_1, ..., c_m]^T\)</span> in the shape of <span class="math inline">\((ld)\times m\)</span>, where <span class="math inline">\(l\)</span> is the number of head; <span class="math inline">\(d\)</span> is feature dimension (<span class="math inline">\(d=512\)</span> in the paper); and <span class="math inline">\(m\)</span> is the number of tokens.</p><h3 id="ffn">4. FFN</h3><p>Two feedfoward network, <span class="math inline">\(d_{ff}=2048\)</span>, to introduce non-linear layers.</p><h1 id="shining-point">Shining point</h1><h3 id="multi-head-attention">1. Multi-head Attention</h3><p>Here is an explaintion from the author,</p><blockquote><p>In the Transformer this is reduced to a constant number of <strong>operations</strong>, albeit at the cost of reduced effective resolution due to averaging attention-weighted positions, an effect we counteract with Multi-Head Attention as described in section 3.2.</p></blockquote><p>The number of operations is not the computation complexity, but sequential operations. It means that we can get the relationships between the word <span class="math inline">\(i\)</span> and word <span class="math inline">\(j\)</span> directly without regard to their positions. In RNN, relationships are accumulated and are implicitly encoded in the hidden states, but at least, they sequentially interact under the heuristic. This is the reason why RNN is good at NLP. For Transformer, QKV allows directly interaction, but no longer sequentially. Suppose a word could have multiple relationships with other words, and most of the related words are at the front. However, it is common that some words are behind, like clauses. Therefore, attention learned by QKV is "averaged" to different scenes. To solve it, the only thing we need to do is use multiple attention, and those attentions will be trained to focus on different patterns. Attention is all you need :) Heuristic is block :( <a href="https://zhuanlan.zhihu.com/p/420269546">r1</a>, <a href="https://zhuanlan.zhihu.com/p/264749298">r2</a></p><p>Meanwhile, the demension of <span class="math inline">\(Q_i\ ,\ K_i\ ,\ V_i\)</span> then reduced to <span class="math inline">\(512/8=64\)</span>, which is finally concatenated back to <span class="math inline">\(512\)</span>-d. So that we can improve the performance of each attention block while preserving the total number of parameters.</p><h1 id="trick">Trick</h1><ul><li>In queation <span class="math inline">\((1)\)</span>, the quthor scaled <span class="math inline">\(QK^T\)</span> by <span class="math inline">\(1/\sqrt{d_k}\)</span>. Large <span class="math inline">\(QK^T\)</span> will push softmax function into regions where it has extremely small gradients.</li></ul><!-- # Journey## background- [-] LSTM is popular at that time, but has disadvantages: factor computation along the symbol positions. This inherently sequential nature reasonably catch up words dependencies, but preclude parallelization training.- [+] Attention mechanisms allow catch up words dependencies without regard to positions.- [o] Attention mechanisms however are in conjunction with RNN in current fashions.**Purpose 1.** Why not throw RNN away? Just use attention.- [-] Sequential computation is heavy.- [+] CNN computation is light. But current models are still computational intensive, which is factored to words distances.**Purpose 2.** Come up a method insetad of convolution, and keep a costant computation without regard to words distances.## solution 1.0**Purpose 1.** Build the Transformer (see Design)**Purpose 2.** averaging attention-weighted positions## challenge 1.0**Purpose 2.** reduced effective resolution## solution 2.0**Purpose 2.** Multi-Head Attention# Design## feature- encoder-decoder style- stacked self-attention- point-wise fully connected layersQ: 当我在decode的时候，用于decode当前单词j的hidden source就叫做Q_iK: encode完成后，每一个单词的hidden embedding就叫做K_iV: encode完成后，每一个# Re-think# My thought --><!-- <div class="markdown-body"></div> -->]]></content>
    
    
    <categories>
      
      <category>Paper review</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NLP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2022/02/01/hello-world/"/>
    <url>/2022/02/01/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="quick-start">Quick Start</h2><h3 id="create-a-new-post">Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><div class="code-wrapper"><pre><span class="line">1</span><br></pre></div></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="run-server">Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="generate-static-files">Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="deploy-to-remote-sites">Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
